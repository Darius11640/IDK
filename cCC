console.log('Hello, World! Let\'s get some basic data from this page.');

// Get the page title
const pageTitle = document.title;
console.log('Page title:', pageTitle);

// Get the first main heading (h1)
const firstHeading = document.querySelector('h1');
if (firstHeading) {
  console.log('First heading text:', firstHeading.textContent.trim());
} else {
  console.log('No h1 found on this page.');
}